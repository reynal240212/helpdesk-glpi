# Frontend (templates)

Este frontend React (Vite) está en:

- `src/templates`

## Ejecutar

```bash
cd src/templates
npm install
npm run dev
```

Abrir en navegador: `http://localhost:5173`

El frontend consume la API de Spring Boot en `http://localhost:8080/api`.
